[build]
  # Build command to run before deploying
  command = "npm run build"
  
  # Directory to deploy (the built output)
  publish = "dist"

# Redirect all requests to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
